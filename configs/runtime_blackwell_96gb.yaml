profile_id: "blackwell-96gb-v1"
hardware: "NVIDIA Blackwell 96 GB"
default:
  dtype: "bfloat16"
  quantization: null
  minimum_free_disk_gb: 75

# Every panel checkpoint (largest: the 27B tier) fits in plain BF16 on this
# profile, so no per-model quantization or disk overrides are needed since the
# Apertus pair moved back to 8B.
models: {}
