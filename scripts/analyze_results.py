#!/usr/bin/env python
"""[PHASE 4-5] Aggregate metrics, tables and figures (spec sections 13, 18).

Mapping metrics + calibration + robustness + emotion-concept metrics + causal
effects, with stratified bootstrap CIs and multiple-comparison correction.
Emits tables to outputs/tables and figures to outputs/figures.

Status: scaffold. Metric + statistics helpers are already available
(oncoemotion.evaluation, oncoemotion.statistics).
"""

import sys


def main() -> int:
    print(__doc__)
    print("Not yet implemented (Phase 4-5). Metric/statistics helpers are ready.")
    return 0


if __name__ == "__main__":
    sys.exit(main())
